import styled from 'styled-components';
import { white } from '../UI/baseLayout';

export const StTicTacToePlate = styled.div`
    width: 312px;
    height: 312px;
    background: ${white};
    display: flex;
    flex-wrap: wrap;
    color: gray; //temporary
    margin: 10px auto;
`;
